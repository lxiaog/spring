spring:
  profiles:
    active: 
    - dev

---
spring:
  profiles: dev
  application:
    name: dept-config-client-dev


---
spring:
  profiles: test
  application:
    name: dept-config-client-test
